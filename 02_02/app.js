const operacion = require('./operaciones');

const resultado = operacion.suma(10, 5);
  
console.log('El resultado de la suma es', resultado);
console.log('El resultado de la resta es', operacion.resta(34,5));
